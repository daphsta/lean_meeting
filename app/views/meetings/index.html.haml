.row
  %br
  .large-9.large-centered.columns
    %table
      %thead
        %tr
          %th Dates
          %th Title
          %th Description
          %th Organiser
          %th Venue
        - @meetings.each do |meeting|
          %tbody
            %tr
              %td
                = (meeting.time).strftime('%d/%m/%Y %H:%M')
              %td
                = meeting.title
              %td
                = meeting.description
